========================================================
HANDOFF — Dashboard Miranda Studio
========================================================

PROJETO: Dashboard Miranda Studio
DATA: 2026-04-10
RESPONSÁVEL: Matheus (setup inicial via Claude)
BRANCH: main (repo ainda não criado no GitHub)

--------------------------------------------------------
1. O que foi feito neste push
--------------------------------------------------------
- Criada estrutura de pastas do projeto (/docs, /src)
- Criados arquivos base de contexto: HANDOFF, STATUS_ATUAL, DECISOES
- Criados arquivos de produção: BACKLOG_MIRANDA, MAPEAMENTOS (esqueletos), MODELAGEM_DADOS, REGRAS_CONSOLIDACAO
- Criado CHECKLIST_VALIDACAO em /docs/qa
- Criado README.md com instruções de revezamento
- Repositório GitHub criado e primeiro push realizado (T1a concluída)
  URL: https://github.com/Dev-Stepads/miranda-estudio-dashboard
- Projeto Supabase criado (T1b concluída)
  URL: https://pntlbaffrelxymiodelj.supabase.co
  Nome: miranda-dashboard
  Org: dev@stepads.com.br
- .env.local criado localmente com as 3 chaves do Supabase
  (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY)
  — confirmado que não é commitado (.gitignore linha 8)
- Criado CLAUDE.md na raiz do repo com as regras obrigatórias
  que qualquer Claude (de qualquer pessoa do revezamento) deve
  seguir: ler HANDOFF antes de mexer, atualizar os 3 docs vivos
  (BACKLOG + HANDOFF + STATUS_ATUAL) quando estado mudar — com
  destaque de que o BACKLOG é o mais esquecido — regras de negócio
  (Visão Geral sem Meta Ads), DECISOES append-only, etc.
- BACKLOG_MIRANDA.txt: T1b marcada como [x] (estava desmarcada por
  esquecimento anterior — corrigido).
- Nenhum código de integração escrito ainda

--------------------------------------------------------
2. O que está funcionando
--------------------------------------------------------
- Estrutura de documentação pronta para ser usada pela equipe
- Backlog documentado com todas as tasks T1–T45
- Repositório GitHub ativo e conectado ao projeto local
- Projeto Supabase ativo e credenciais configuradas localmente
- .gitignore protege .env.local (confirmado: não foi commitado)

--------------------------------------------------------
3. O que ainda não está pronto
--------------------------------------------------------
- Vercel do projeto (T1c — criar com dev@stepads.com.br)
- Confirmar região do Supabase (deve ser South America / São Paulo)
- Mapeamento técnico das 3 APIs (Conta Azul, Nuvemshop, Meta Ads)
- Toda a implementação de código

--------------------------------------------------------
4. Bloqueios encontrados
--------------------------------------------------------
- Nenhum no momento. Aguardando criação de Supabase e Vercel.

--------------------------------------------------------
5. Próximo passo exato para quem assumir
--------------------------------------------------------
Completar o restante da Etapa 1 nesta ordem:

  1. T1c — Criar projeto Vercel (dev@stepads.com.br)
     - importar o repo Dev-Stepads/miranda-estudio-dashboard
     - adicionar as 3 env vars do Supabase em Environment Variables:
         SUPABASE_URL=https://pntlbaffrelxymiodelj.supabase.co
         SUPABASE_ANON_KEY=(copiar do .env.local local)
         SUPABASE_SERVICE_ROLE_KEY=(copiar do .env.local local)
  2. Verificar região do Supabase (Settings → Infrastructure).
     Deve ser South America (São Paulo). Se não for, avaliar recriar
     antes de popular o banco.
  3. Atualizar STATUS_ATUAL.txt com URL da Vercel quando pronta.
  4. Iniciar T4–T6 (mapeamento técnico das 3 APIs) — preencher os arquivos
     em /docs/producao/MAPEAMENTO_*.txt

Depois disso, seguir com T7 (autenticação Conta Azul).

--------------------------------------------------------
6. Arquivos principais alterados
--------------------------------------------------------
- Toda a estrutura inicial de /docs e /src (primeira vez)
- README.md

--------------------------------------------------------
7. Como testar
--------------------------------------------------------
- Ainda não há código executável. Validação nesta fase = ler os docs e confirmar que o backlog está claro.

--------------------------------------------------------
8. Observações para o Claude
--------------------------------------------------------
- Contexto importante: projeto será revezado entre várias pessoas. Ler HANDOFF antes de qualquer alteração é OBRIGATÓRIO.
- Regra de negócio importante: Visão Geral = Loja Física + Nuvemshop consolidados. NÃO inclui Meta Ads no faturamento (Meta Ads é atribuição, não venda).
- Cuidado ao alterar: não mexer em /docs/context sem ler o que já está lá. DECISOES.txt é append-only — nunca apagar decisões antigas, apenas marcar como revertidas se necessário.
- Segurança: credenciais de login do Conta Azul e Nuvemshop foram compartilhadas em chat e devem ser trocadas pelo usuário. Nunca commitar .env.
