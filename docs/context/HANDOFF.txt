========================================================
HANDOFF — Dashboard Miranda Studio
========================================================

PROJETO: Dashboard Miranda Studio
DATA: 2026-04-12
RESPONSAVEL: Matheus (sessao longa 2026-04-12 via Claude)
BRANCH: main

--------------------------------------------------------
1. O que foi feito neste push
--------------------------------------------------------
SESSAO MONSTRO 2026-04-12 — ETL real + Frontend completo

  ## ETL Nuvemshop (COMPLETO — dados reais fluindo)

  * Full sync executado: 5928 orders + 4913 customers + 43 checkouts
    + ~10k sale_items inseridos no Supabase
  * Modo incremental implementado: 12s vs 10min do full sync
  * 5+ fixes em Zod schemas para campos nullable descobertos em dados
    reais (address, billing, contact, price, total, I18n, completed_at)
  * total_price fix: fallback price x quantity quando API omite o campo
  * Views DROP + CREATE: v_*_produtos agora agrupa por
    sale_items.product_name em vez de products.product_id (97% dos items
    tinham product_id null porque Miranda nao preenche SKU no catalogo)
  * Arquivos: src/etl/nuvemshop/sync.ts, run.ts

  ## ETL Conta Azul (COMPLETO — dados reais fluindo)

  * T45 RESOLVIDA: descoberto que GET /v1/notas-fiscais/{chave_acesso}
    retorna XML (nao JSON). Parser implementado com fast-xml-parser.
  * Full sync 1 ano: 545 NF-e + 1196 sale_items inseridos no Supabase
  * Modo incremental implementado: 10s vs 8min do full sync
  * Janela de 15 dias max no endpoint de lista — ETL faz chunking em
    janelas de 14 dias automaticamente
  * normalizeState() criado em src/lib/brazil.ts — mapeia "Bahia" -> "BA"
    etc. com 93.5% de cobertura
  * Refresh_token persistence resolvida via tabela etl_config no Supabase
  * Arquivos: src/etl/conta-azul/sync.ts, run.ts,
    src/integrations/conta-azul/nfe-parser.ts

  ## Frontend Next.js (COMPLETO — 4 abas funcionando)

  * Next.js 16 + App Router + Tailwind 4 + Recharts
  * Rodando localmente em localhost:3000
  * 4 abas implementadas:
    - Visao Geral (faturamento total, donut canal, ranking produtos)
    - Nuvemshop (faturamento, produtos, geografia, abandonados)
    - Loja Fisica (faturamento, produtos, demografia por estado)
    - Meta Ads (placeholder — aguardando desbloqueio humano)
  * Componentes: KpiCard (com badges de % change), RevenueChart (bar),
    ChannelDonut (pie), GeographyChart, TopProductsTable, SimpleTable,
    NavTabs, PeriodFilter
  * Period filter (7d/15d/30d/90d/1ano) via URL search params, funciona
    em todas as abas
  * KPI comparison: busca 2x periodo e calcula % change vs periodo anterior
  * force-dynamic em todas as paginas de dados (sem stale cache)
  * service_role em Server Components (seguro pq e server-only code)

  ## Infraestrutura

  * GitHub Actions cron rodando a cada 30 minutos (sync Nuvemshop + Conta Azul)
  * Arquivo: .github/workflows/sync-nuvemshop.yml
  * etl_config table no Supabase para persistencia do refresh_token
  * Migration: supabase/migrations/20260412150000_etl_config_table.sql
  * Migration: supabase/migrations/20260412120000_fix_product_views_group_by_name.sql
  * npm scripts: dev, build, sync:nuvemshop, sync:nuvemshop:full,
    sync:contaazul, sync:contaazul:full
  * Deps adicionadas: @supabase/supabase-js, fast-xml-parser, next,
    react, recharts

  ## Dados reais no Supabase

  * customers: 5441
  * sales: 6493
  * sale_items: 11293
  * abandoned_checkouts: 48
  * etl_config: 1 (refresh_token)
  * products: 10 (seed only — Miranda nao preenche SKU, documentado
    em PENDENCIAS_MIRANDA.txt)

  ## Novos arquivos criados

  * PENDENCIAS_MIRANDA.txt (7 itens Miranda + 1 item interno I1)
  * src/lib/brazil.ts, src/lib/supabase.ts
  * src/etl/nuvemshop/sync.ts, run.ts
  * src/etl/conta-azul/sync.ts, run.ts
  * src/integrations/conta-azul/nfe-parser.ts
  * src/scripts/explore-conta-azul.ts, fetch-nfe-xml.ts, debug-nfe-list.ts
  * app/ — full Next.js frontend (layout, pages, components)
  * .github/workflows/sync-nuvemshop.yml
  * supabase/migrations/20260412120000_fix_product_views_group_by_name.sql
  * supabase/migrations/20260412150000_etl_config_table.sql

--------------------------------------------------------
2. O que esta funcionando
--------------------------------------------------------
- ETL Nuvemshop: full sync + incremental, rodando via GitHub Actions cron
  a cada 30 min. 5928 orders, 4913 customers, 43 checkouts reais no banco.
- ETL Conta Azul: full sync + incremental, rodando via GitHub Actions cron
  a cada 30 min. 545 NF-e, 1196 sale_items reais no banco.
  Refresh_token persistido via etl_config table (resolve CI/cron).
- Frontend Next.js 16: 4 abas (Visao Geral, Nuvemshop, Loja Fisica,
  Meta Ads placeholder) com period filter e KPI comparison funcionando
  em localhost:3000.
- Supabase: 6493 sales, 11293 sale_items, 5441 customers, 48 abandoned
  checkouts — tudo real da Miranda.
- Views do dashboard corrigidas: agrupam por product_name (nao product_id).
- GitHub Actions: cron a cada 30 min sincronizando ambas as fontes.
- 109+ unit tests passando (base Caminho B).
- Conta Azul + Nuvemshop auth totalmente operacionais.

--------------------------------------------------------
3. O que ainda nao esta pronto
--------------------------------------------------------
- Meta Ads (bloqueado por 2 lados humanos):
  * Conta dev@stepads.com.br em validacao no developers.facebook.com
  * Miranda precisa fornecer: BM ID, Ad Account ID, acesso ao BM
  * Nenhum progresso tecnico possivel ate esses bloqueios sairem

- Webhooks LGPD Nuvemshop (T44):
  * 3 URLs placeholder cadastradas mas rotas NAO existem no Vercel
  * Critico antes de producao real com dados de clientes fluindo

- Deploy Vercel do frontend:
  * Frontend funciona localmente, falta validar deploy no Vercel
  * Env vars de Supabase ja estao configuradas no Vercel

- QA/Validacao (T36-T42):
  * Nenhuma validacao formal feita ainda
  * Faturamento precisa ser cruzado com dados reais da Miranda

- Cofre de senhas Stepads (T46):
  * .env.local no disco do Matheus e a fonte da verdade interina
  * Bloqueia revezamento seguro

- Secrets que passaram pelo chat (T47):
  * Devem ser rotacionados antes de producao real

- Regras de consolidacao de produtos (T29):
  * SKU nao e usado pela Miranda na Nuvemshop
  * Consolidacao por nome e o fallback atual

- Gates pendentes com representante Miranda:
  * Checkout Nuvemshop pede genero? (Gate N#1)
  * Custom field de birthday? (Gate N#2)
  * partially_paid no faturamento? (recomendacao = sim)
  * Attribution window do Meta Ads

--------------------------------------------------------
4. Bloqueios encontrados
--------------------------------------------------------
- META ADS: conta dev@stepads.com.br em validacao + Miranda nao
  respondeu com BM ID / Ad Account ID / acesso ao BM.
- COFRE DE SENHAS: proximo do revezamento nao consegue credenciais
  sem pedir ao Matheus (T46).
- LGPD: webhooks placeholder vao retornar 404 se Nuvemshop pingar.
  Nao e urgente enquanto nao houver dados fluindo em producao, mas
  precisa ser resolvido antes do go-live.

--------------------------------------------------------
5. Proximo passo exato para quem assumir
--------------------------------------------------------
ANTES DE COMECAR:
  - git pull
  - cp .env.example .env.local (preencher com credenciais do cofre
    ou pedir ao Matheus)
  - npm install
  - npm test (validar 109+ tests)
  - npm run dev (frontend em localhost:3000)

PROXIMO PASSO IMEDIATO — 3 frentes abertas por prioridade:

  FRENTE 1 — Deploy Vercel + QA (RECOMENDACAO FORTE)
    O frontend esta pronto localmente. Proximo:
    1.1. Fazer push do codigo frontend e validar deploy no Vercel
    1.2. Testar as 4 abas com dados reais no ambiente de producao
    1.3. Cruzar faturamento do dashboard com relatorios reais da Miranda
         (QA tasks T36-T42)
    1.4. Ajustar filtros/KPIs conforme feedback

  FRENTE 2 — Webhooks LGPD (T44) + robustez do ETL
    2.1. Criar 3 API routes Next.js para LGPD:
         /api/webhooks/nuvemshop/lgpd/store-redact
         /api/webhooks/nuvemshop/lgpd/customers-redact
         /api/webhooks/nuvemshop/lgpd/customers-data-request
    2.2. Validar HMAC com NUVEMSHOP_CLIENT_SECRET
    2.3. Implementar logica de DELETE/export conforme MAPEAMENTO_NUVEMSHOP.txt §A9
    2.4. Monitorar GitHub Actions cron — garantir que ETL incremental
         esta rodando sem erros

  FRENTE 3 — Meta Ads (BLOQUEADO — aguardando humanos)
    Nao comecar ate:
    (a) Conta Facebook dev sair da validacao
    (b) Miranda fornecer BM ID + Ad Account ID + acesso ao BM

  PENDENCIAS MIRANDA (ver PENDENCIAS_MIRANDA.txt):
    7 itens pendentes de acao da Miranda + 1 item interno (I1 filter)

  GATES COM REPRESENTANTE (nao bloqueiam trabalho tecnico):
    - Genero no checkout Nuvemshop (Gate N#1)
    - Birthday custom field (Gate N#2)
    - Attribution window Meta Ads

--------------------------------------------------------
6. Arquivos principais alterados
--------------------------------------------------------
- src/etl/nuvemshop/sync.ts, run.ts (ETL real)
- src/etl/conta-azul/sync.ts, run.ts (ETL real)
- src/integrations/conta-azul/nfe-parser.ts (XML parser)
- src/lib/brazil.ts (normalizeState)
- src/lib/supabase.ts (client helper)
- app/ (full Next.js frontend)
- .github/workflows/sync-nuvemshop.yml (cron)
- supabase/migrations/20260412*.sql (2 migrations)
- PENDENCIAS_MIRANDA.txt (novo)
- Schemas Zod corrigidos (5+ nullable fixes)

--------------------------------------------------------
7. Como testar
--------------------------------------------------------
- npm test — 109+ unit tests
- npm run dev — frontend em localhost:3000 (precisa .env.local com
  SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY)
- npm run sync:nuvemshop — sync incremental Nuvemshop
- npm run sync:nuvemshop:full — sync completo Nuvemshop
- npm run sync:contaazul — sync incremental Conta Azul
- npm run sync:contaazul:full — sync completo Conta Azul

--------------------------------------------------------
8. Observacoes para o Claude
--------------------------------------------------------
- REGRA: Visao Geral = Loja Fisica + Nuvemshop. NAO inclui Meta Ads.
- REGRA: DECISOES.txt e append-only.
- CUIDADO: NF-e do Conta Azul retorna XML, nao JSON. Parser em
  src/integrations/conta-azul/nfe-parser.ts.
- CUIDADO: 15 dias max na janela do endpoint de lista de NF-e.
  ETL faz chunking em 14 dias.
- CUIDADO: 97% dos sale_items tem product_id null. Views agrupam
  por product_name, nao por product_id.
- CUIDADO: refresh_token do Conta Azul e single-use. Persistencia
  via etl_config table no Supabase.
