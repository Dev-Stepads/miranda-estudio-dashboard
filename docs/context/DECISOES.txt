========================================================
DECISÕES — Dashboard Miranda Studio
========================================================

Este arquivo registra decisões técnicas e de negócio
que afetam o projeto. É APPEND-ONLY: nunca apague uma
decisão. Se ela for revertida, adicione uma nova entrada
marcando como "REVERTIDA" e explique o motivo.

Formato de cada entrada:
  [YYYY-MM-DD] [CATEGORIA] Título curto
    Contexto: por que essa decisão foi tomada
    Decisão:  o que foi decidido
    Impacto:  o que muda no projeto por causa disso

Categorias possíveis:
  NEGOCIO | ARQUITETURA | DADOS | INTEGRACAO | UX | QA | SEGURANCA

========================================================
DECISÕES REGISTRADAS
========================================================

[2026-04-10] [ARQUITETURA] Stack do projeto
  Contexto: precisávamos definir hospedagem de backend, banco e frontend
            para um dashboard que consome 3 APIs externas.
  Decisão:  GitHub (repo) + Supabase (banco + funções) + Vercel (deploy do dashboard).
            Contas operacionais: dev@stepads.com.br (Supabase + Vercel).
  Impacto:  Todo o ETL pode rodar em Supabase Edge Functions ou cron jobs.
            Dashboard será uma SPA/Next.js hospedada na Vercel.

[2026-04-10] [NEGOCIO] Escopo da Visão Geral
  Contexto: precisávamos definir o que entra no faturamento consolidado.
  Decisão:  Visão Geral consolida APENAS Loja Física (Conta Azul) +
            E-commerce (Nuvemshop). Meta Ads NÃO entra no faturamento
            consolidado — entra apenas como atribuição na aba própria.
  Impacto:  Evita double-counting. Faturamento atribuído do Meta Ads
            é um recorte informativo, não soma ao total do negócio.

[2026-04-10] [DADOS] Estado do cliente por DDD na Loja Física
  Contexto: Conta Azul pode não trazer estado do cliente direto.
  Decisão:  Derivar estado pelo DDD do telefone quando não houver campo
            de endereço preenchido. Documentar regra de fallback em
            /docs/producao/REGRAS_CONSOLIDACAO.txt.
  Impacto:  Clientes sem telefone ou com DDD inválido entram como
            "Desconhecido" na análise geográfica da Loja Física.

[2026-04-10] [PROCESSO] Revezamento via HANDOFF.txt
  Contexto: projeto será tocado por múltiplas pessoas + Claude em sessões
            distintas, sem memória compartilhada.
  Decisão:  Todo push relevante exige atualização de HANDOFF.txt.
            Antes de qualquer alteração, é obrigatório ler HANDOFF +
            STATUS_ATUAL + mapeamento da fonte envolvida.
  Impacto:  Zero perda de contexto entre sessões. Cria pequeno overhead
            em cada push, mas é intencional.

[2026-04-10] [SEGURANCA] Nunca armazenar credenciais em memória ou em git
  Contexto: credenciais de login (Conta Azul, Nuvemshop) foram
            compartilhadas em chat durante o setup.
  Decisão:  Credenciais ficam apenas em .env.local (gitignored) e em
            gerenciador de senhas. Usuário deve trocar as senhas
            compartilhadas em chat. Para as APIs, usar OAuth/tokens,
            nunca login/senha do painel.
  Impacto:  .gitignore precisa cobrir .env*. Toda nova integração deve
            usar variáveis de ambiente.

[2026-04-10] [INTEGRACAO] Usar API NOVA do Conta Azul (v1), não a legada
  Contexto: Conta Azul mantém duas APIs em paralelo. A legada
            (api.contaazul.com.br/pub/...) usa ExternalApplicationToken
            + CompanyToken. A nova (api.contaazul.com/v1/...) usa
            OAuth 2.0 padrão e foi lançada em março/2025. O prazo
            oficial de migração para a nova era 2025-11-05, já vencido.
  Decisão:  Todo o desenvolvimento da integração Conta Azul usa
            EXCLUSIVAMENTE a API nova v1. Aplicação registrada em
            https://developers-portal.contaazul.com/ com credenciais
            client_id + client_secret. Fluxo OAuth = authorization_code.
  Impacto:  Refresh token é single-use (2 semanas de validade) — exige
            persistência transacional no Supabase para evitar race
            condition. Access token dura 1 hora. Sem sandbox dedicado:
            testes usam conta trial de 3 dias ou produção do cliente.
            Sem webhooks — sincronização via polling em cron.

[2026-04-10] [INTEGRACAO] ETL do Conta Azul será 100% polling
  Contexto: API nova do Conta Azul não suporta webhooks. Precisamos
            manter os dashboards razoavelmente atualizados ao longo
            do dia sem depender de push do lado da Conta Azul.
  Decisão:  Cron jobs alinhados ao Supabase (ou Vercel Cron com região
            gru1) executando:
              - ingestão incremental a cada 30 min em horário comercial
              - ingestão completa do dia anterior uma vez por dia (02h)
            Preferência: rodar no Supabase Edge Functions (colocalizado
            com o banco em São Paulo) para evitar possível geo-bloqueio
            da API Conta Azul fora do Brasil.
  Impacto:  Rate limit 600 req/min × N chamadas /sales/{id}/items por
            venda (endpoint separado) é o maior risco de performance.
            Na T7 validar se existe param "expand=items" que permita
            trazer items inline em /sales — impacto direto no desenho
            do loop de ingestão.

[2026-04-10] [ESCOPO] [NEGOCIO] Loja Física sem demografia de gênero/idade
  Contexto: Validação direta no dashboard real da Miranda em 2026-04-10
            (screenshots da ficha de cliente real e das abas
            "Informações adicionais") confirmou que o cadastro de
            cliente do Conta Azul usado pela Miranda NÃO tem campo de
            gênero nem de data de nascimento. Miranda NÃO usa campos
            personalizados / custom fields.
  Decisão:  A aba "Loja Física" do dashboard NÃO terá análise por
            gênero nem por faixa etária. Essas análises ficam APENAS
            na aba Nuvemshop, onde os dados existem.
            A aba Loja Física terá:
              - Faturamento (total e por período)
              - Ranking de produtos vendidos
              - Ticket médio
              - Distribuição geográfica por estado (endereço do cliente
                ou DDD como fallback, já que o estado vem direto no
                cadastro do cliente)
  Impacto:  1. MODELAGEM_DADOS.txt: a tabela `customers` continua tendo
               os campos gender/age_range/age (são canônicos entre
               fontes), mas para `source = 'conta_azul'` esses campos
               ficam sempre em 'unknown'.
            2. REGRAS_CONSOLIDACAO.txt: a regra §4 (demografia) precisa
               deixar explícito que Conta Azul não fornece gênero/idade.
            3. DASHBOARD / Views: v_loja_fisica_demografia_genero e
               v_loja_fisica_demografia_idade são REMOVIDAS da
               modelagem. v_loja_fisica_estado permanece.
            4. README.md / descrição das abas: atualizar para refletir
               a ausência dessas métricas na aba Loja Física.
            Caminhos abertos para o futuro (não vão ser feitos agora):
              - Miranda adicionar campos personalizados no cadastro
                (só resolveria clientes novos, não o histórico)
              - Enriquecer via fonte externa (CRM / WhatsApp / etc.)
            Essas opções ficam registradas como ideias para uma
            eventual evolução do dashboard. Por agora, escopo fechado.

[2026-04-10] [INTEGRACAO] Separar canal de venda por "vendedor_responsavel"
  Contexto: Investigação no dashboard real do Conta Azul da Miranda
            (venda #16138, 2026-04-10) mostrou um campo "Vendedor
            responsável" com valor "LOJA FÍSICA". Esse campo é usado
            como etiqueta de canal pela Miranda — todas as vendas
            do ponto físico carregam esse vendedor. A tela "Vendas de
            produtos" lista todas as vendas (incluindo PDV/frente de
            caixa) numa única lista, então não há endpoint separado
            de PDV na API.
  Decisão:  O ETL do Conta Azul vai filtrar vendas da loja física
            usando o campo "vendedor_responsavel" = "LOJA FÍSICA"
            (ou valores equivalentes a confirmar na T7/T8).
  Impacto:  A query canônica para sales da Conta Azul vai ter um filtro
            adicional por vendedor. Precisamos mapear na T7 a LISTA
            COMPLETA de valores usados pela Miranda nesse campo — se
            houver variações tipo "LOJA FÍSICA - CAIXA 1", ou
            vendedores individuais para comissão, o filtro precisa
            acomodar todos. Em caso de dúvida, puxar TUDO e filtrar
            no ETL (mais seguro que depender de query filter externo).

[2026-04-10] [INTEGRACAO] Nuvemshop — webhooks primários + polling safety-net
  Contexto: A API Nuvemshop suporta webhooks (order/paid, order/cancelled,
            customer/created, etc.) com política de retry de 48h. Webhook
            sozinho pode perder eventos se nosso endpoint ficar fora do ar
            por mais de 48h. Polling sozinho desperdiça rate limit e cria
            latência desnecessária.
  Decisão:  Híbrido. Webhook como caminho primário para eventos de order
            e customer. Polling defensivo:
              - /orders com janela das últimas 48h (1x/dia, reconciliação)
              - /checkouts a cada 6h (carrinhos abandonados — retenção
                de 30 dias exige frequência mínima)
            Webhooks chegam via endpoint
              https://miranda-estudio-dashboard.vercel.app/api/webhooks/nuvemshop
            Validação HMAC-SHA256 obrigatória usando o client_secret
            como chave. Processamento é assíncrono (responde 200 em
            menos de 3s, enfileira o resource_id para um worker).
  Impacto:  Arquitetura do ETL da Nuvemshop é diferente da do Conta Azul
            (que é 100% polling por falta de webhooks). Código dos dois
            não deve ser reusado direto — mas a normalização final para
            tabelas canônicas (sales, customers) é a mesma.

[2026-04-10] [DADOS] Data canônica da venda por fonte
  Contexto: REGRAS_CONSOLIDACAO.txt §1.2 deixou em aberto qual campo de
            data usar ("decidir entre data do pedido x data do pagamento").
            O T4 e o T5 deram informação suficiente pra resolver.
  Decisão:  - Conta Azul: "Data de venda" (campo já visto na UI, mapear
                           para nome exato na API em T7).
            - Nuvemshop:  paid_at quando disponível, senão created_at.
                          A maioria dos relatórios de e-commerce usa
                          data de pagamento e é o que o dashboard deve
                          refletir pra não confundir o cliente.
            - Meta Ads:   date_start do insight (era a regra original).
            Timezone: America/Sao_Paulo para todos os 3.
  Impacto:  Atualizar REGRAS_CONSOLIDACAO.txt §1.2 pra refletir isso.
            No caso da Nuvemshop, vendas pending/authorized que ainda
            não foram pagas entram no dashboard com a data em que foram
            criadas — depois, quando payment_status virar "paid", o
            registro canônico é REESCRITO com paid_at. Idempotência
            do ETL precisa cuidar disso.

[2026-04-10] [ESCOPO] Gênero/idade do dashboard agora dependem APENAS
            da Nuvemshop — risco aberto
  Contexto: Com Conta Azul confirmado sem esses campos (T4), e o
            mapeamento da Nuvemshop (T5) mostrando que gender é
            campo padrão do extra mas só é preenchido se a loja
            pedir no checkout, e que birthday é custom field que a
            loja precisa criar, a análise demográfica do dashboard
            INTEIRO depende de duas configurações no painel da
            Nuvemshop da Miranda.
  Decisão:  Antes de iniciar T13 (auth Nuvemshop) ou T18-T20 (coleta
            de demografia Nuvemshop), o Matheus vai:
              1. Verificar se o checkout da Nuvemshop da Miranda
                 pede gênero.
              2. Verificar se existe custom field de data de
                 nascimento/birthday configurado.
            Se QUALQUER um dos dois estiver desativado:
              a) Pedir pra Miranda ativar, OU
              b) Aceitar que essa análise sai do escopo do dashboard
                 inteiro (não só da Loja Física).
  Impacto:  Gate crítico pra metade das métricas demográficas do
            projeto. Precisa ser resolvido antes de qualquer linha
            de código T13 ser escrita.

[2026-04-10] [INTEGRACAO] Meta Ads: usar System User Token (não expira)
  Contexto: A Marketing API do Meta oferece 3 tipos de token:
            user short-lived (1-2h), user long-lived (60d) e System
            User token (não expira). Para ETL 24/7 em produção, a
            única opção viável é System User.
  Decisão:  Usar System User access token configurado no Business
            Manager da Miranda, com scopes ads_read e
            business_management. Token gerado via:
              Business Settings → System Users → Generate New Token
              Opção "set_token_expires_in_60_days": false
            Ad Account assinada ao System User com permissão
            "View Performance" ou "View Campaigns".
  Impacto:  Requer coordenação com a Miranda para criar System User
            no BM deles. Se não puderem, alternativa é long-lived
            user token (60d) com rotina de refresh. Preferência
            é System User pra simplicidade do ETL.

[2026-04-10] [DADOS] Meta Ads: attribution window = 7d_click + 1d_view
  Contexto: Em 2026-01-12 o Meta REMOVEU 7d_view e 28d_view do Ads
            Insights API. Janelas disponíveis agora: 1d_click,
            1d_view, 7d_click. Default do Meta (quando não passamos
            action_attribution_windows) é 7d_click + 1d_view.
  Decisão:  Todo o ETL passa explicitamente
              action_attribution_windows=['7d_click','1d_view']
            Isso bate com o que a Miranda vê no Ads Manager por
            default, e os números do nosso dashboard vão ser
            consistentes com o painel nativo do Meta.
  Impacto:  Se a Miranda usa janela diferente (ex: só 1d_click para
            ser conservador na atribuição), os números vão divergir
            do dashboard deles. Validar com o representante antes
            de T25. Se divergir, trocar a janela no código.
            Observação: não podemos oferecer 7d_view nem 28d_view
            mesmo que o cliente peça — foram desligados pelo Meta.

[2026-04-10] [INTEGRACAO] Meta Ads: ETL 100% polling a cada 30 min
  Contexto: Marketing API tem webhooks, mas eles só cobrem EVENTOS
            (status change, ad rejected, account issues). Para
            métricas de insights (spend, impressões, conversões),
            webhooks NÃO são uma opção. A Ads Reporting API
            atualiza os dados do lado do Meta a cada 15 minutos.
  Decisão:  ETL polling em /insights:
              - Frequência: a cada 30 min
              - Níveis puxados: campaign, adset, ad (3 chamadas)
              - Janela: últimos 7 dias com UPSERT
                (Meta recalcula histórico por alguns dias)
            Não usamos webhooks do Meta no escopo inicial.
  Impacto:  Dashboard tem latência de até 30-45 min em relação ao
            Ads Manager (15 min do Meta + 30 min do nosso cron).
            Aceitável para todos os KPIs que o cliente pediu.

[2026-04-10] [ARQUITETURA] Travar versão v25.0 da Graph API do Meta
  Contexto: Meta deprecia versões com frequência (ex.: 7d_view removido
            em janeiro de 2026 foi "silent deprecation"). Usar "latest"
            implícito é armadilha.
  Decisão:  Travar todas as chamadas em v25.0 explicitamente na URL.
            Revisão trimestral: checar changelog, testar v25.0+1,
            atualizar se não houver breaking changes.
  Impacto:  Código fica explícito (base URL = graph.facebook.com/v25.0),
            mas obriga rotina de revisão. Alternativa seria pinar em
            envvar para trocar sem deploy, mas por simplicidade,
            fica hardcoded com comentário indicando data de revisão.

[2026-04-11] [DADOS] Convenções do schema inicial do Supabase
  Contexto: primeira migration cria as tabelas raw_* (staging) e
            canônicas (sales, sale_items, products, customers,
            abandoned_checkouts, meta_ads_insights). Várias escolhas
            de tipo e convenção afetam tudo que vem depois.
  Decisão:  1) PKs são bigserial (bigint auto-incrementado), NÃO
               uuid. É mais denso, mais rápido pra joins, e o
               workload não precisa de IDs imprevisíveis publicamente.
               IDs de fonte externa (source_sale_id etc.) ficam em
               coluna separada tipo text.
            2) Enums são text + CHECK constraint, NÃO tipo ENUM
               nativo do PG. Motivo: adicionar valor num ENUM nativo
               exige ALTER TYPE, enquanto CHECK é alterado em uma
               migration comum. As APIs que consumimos podem
               introduzir status novo a qualquer momento.
            3) Raw tables guardam o payload completo da API em uma
               coluna jsonb + algumas colunas pulled-out (source_id,
               date) pra indexar. Append-only. Isso permite reprocessar
               o canônico a partir do histórico sem precisar rechamar
               as APIs.
            4) Todos os timestamps são timestamptz (UTC on disk).
               Conversão pra America/Sao_Paulo acontece na view ou
               aplicação. Datas puras (Meta insights diários) usam date.
            5) Valores monetários são numeric(14,2). Suficiente pra
               999 bi com centavo — overkill, mas sem custo.
            6) RLS habilitado em TODAS as tabelas desde o início,
               sem policies. Resultado: anon/authenticated são negados
               por padrão; ETL usa service_role que tem bypass. Quando
               o frontend começar a ler dados, policies de leitura
               entram em views dedicadas, nunca nas tabelas raw.
            7) SKU em products é único só quando não-nulo (partial
               unique index). Produtos sem SKU podem coexistir, o
               que é comum no catálogo da Miranda.
            8) meta_ads_insights tem unique (date, level, campaign_id,
               coalesce(adset_id,''), coalesce(ad_id,'')) — truque
               pra unique com colunas nullable.
  Impacto:  Define o "jeito Miranda" de modelar. Qualquer tabela
            nova deve seguir esse padrão (RLS on, timestamptz, bigserial,
            numeric pra dinheiro, CHECK pra enum). Migrations futuras
            podem referenciar esta decisão em vez de reargumentar.

[2026-04-11] [INTEGRACAO] Conta Azul — autenticação é AWS Cognito, não OAuth com scopes granulares
  Contexto: durante o registro da app de produção no portal
            developers-portal.contaazul.com (sessão 2026-04-11),
            descobrimos que o que o Conta Azul chama de "OAuth 2.0" é,
            na prática, um wrapper sobre AWS Cognito (issuer
            cognito-idp.sa-east-1.amazonaws.com/sa-east-1_Vp83J11wA,
            confirmado decodificando o JWT do access_token).
            Os scopes não são "sales:read", "customers:read" como o
            MAPEAMENTO_CONTA_AZUL.txt §1 supôs ([VER]). Os REAIS são:
              openid profile aws.cognito.signin.user.admin
            Não há tela de seleção de scopes no portal — é fixo.
  Decisão:  - Aceitar que a autorização granular vem do PERFIL DO ERP
              USER que loga no fluxo OAuth, não dos scopes. Ou seja,
              o que a app pode ler depende das permissões do usuário
              da Miranda que autorizar a app, não do que a gente pediu.
            - Atualizar MAPEAMENTO_CONTA_AZUL.txt §1 (passar de [VER]
              pra [OK] com os scopes Cognito reais).
            - No código (T7), passar exatamente esses 3 scopes na URL
              de authorize: openid+profile+aws.cognito.signin.user.admin
  Impacto:  Não dá pra restringir privilégios da app via scopes. A
            única "defesa" é orientar a Miranda a autorizar com um
            usuário ERP que tenha só permissão de leitura. Documentar
            isso no checklist de onboarding do cliente quando a app
            for pra produção real.

[2026-04-11] [INTEGRACAO] Conta Azul — base URL real é api-v2.contaazul.com
  Contexto: o curl de exemplo que o portal fornece após gerar o
            access_token aponta pra https://api-v2.contaazul.com/v1/
            Nosso MAPEAMENTO_CONTA_AZUL.txt §2 dizia
            https://api.contaazul.com/v1/ (deduzido de fontes
            terceiras, sem confirmação visual no portal oficial).
  Decisão:  Base URL canônica do código é
              https://api-v2.contaazul.com/v1
            Persistido em CONTA_AZUL_API_BASE no .env.local.
            MAPEAMENTO_CONTA_AZUL.txt §2 será atualizado.
  Impacto:  Todas as chamadas HTTP do client Conta Azul devem usar
            essa base. O subdomínio "api-v2" sugere que existe uma
            "api-v1" antiga deprecada — ficar atento se aparecer
            notícia de migração v2→v3 no futuro.

[2026-04-11] [INTEGRACAO] Conta Azul — estratégia DEV app + PROD app (1 redirect por app)
  Contexto: o portal do Conta Azul permite apenas UM redirect_uri
            por aplicação registrada (verificado visualmente em
            2026-04-11). Não há campo "+ adicionar outra URL".
            A gente precisa de redirects diferentes pra
            desenvolvimento local (localhost) e produção (Vercel).
  Decisão:  Usar DUAS aplicações distintas:
            (1) App DEV — auto-criada pelo portal quando você se
                cadastra. Vem com redirect_uri https://contaazul.com
                (placeholder pra dev copiar o ?code= manualmente da
                barra de endereço) e um ERP user de teste com trial
                de ~3 dias. Credenciais salvas como
                CONTA_AZUL_DEV_* no .env.local. Usada para testar
                o fluxo OAuth durante o desenvolvimento do código.
            (2) App PROD — "Miranda Dashboard" criada manualmente
                em 2026-04-11. CNPJ Stepads (32.972.296/0001-07).
                redirect_uri =
                  https://miranda-estudio-dashboard.vercel.app/api/auth/contaazul/callback
                Credenciais salvas como CONTA_AZUL_* (sem _DEV).
                Usada quando a Miranda autorizar de verdade (T7+).
  Impacto:  Código do client OAuth Conta Azul deve ler as variáveis
            CONTA_AZUL_DEV_* em modo dev e CONTA_AZUL_* em prod
            (controlado por NODE_ENV ou flag explícita). Mappers,
            HTTP base e tipos são iguais — só client_id, secret e
            redirect_uri trocam. Cliente secret do Conta Azul é
            visível livremente ao reabrir a app no portal (não é
            one-shot), o que reduz o stress operacional.

[2026-04-11] [DADOS] Views do dashboard — granularidade e convenções
  Contexto: precisávamos decidir o shape das 14 views que alimentam
            as 4 abas do dashboard. Duas opções principais: views
            single-row com KPIs agregados (uma view por card) ou
            views granulares (diário/por entidade) com frontend
            agregando. Também: como lidar com filtro de período,
            timezone e segurança.
  Decisão:  1) GRANULARIDADE DIÁRIA OU POR ENTIDADE.
               Todas as views retornam linhas granulares (por dia,
               por produto, por estado, por gênero, etc.), NUNCA
               single-row KPIs. Razão: PostgreSQL views não aceitam
               parâmetros — se a view agregasse "total do período",
               o período teria que ser hardcoded ou virar function.
               Com granular + WHERE no frontend, o mesmo dataset
               serve "hoje", "últimos 7d", "mês atual" etc. PostgREST
               (camada Supabase) faz o pushdown automático.
            2) KPIs SINGLE-VALUE SÃO CALCULADOS NO FRONTEND.
               total_revenue, avg_ticket, share_canal_pct, ROAS
               consolidado, etc., são derivados das agregações
               diárias via SUM/AVG no client. Menos views, mais
               flexibilidade de filtro.
            3) TIMEZONE APLICADO NA VIEW.
               `(sale_date at time zone 'America/Sao_Paulo')::date`
               transforma timestamptz em date no fuso correto. O
               frontend consome só `day date` e nunca precisa se
               preocupar com conversão.
            4) FILTRO DE REVENUE = status='paid' APENAS.
               'cancelled', 'refunded', 'pending' ficam de fora.
               'partially_paid' da Nuvemshop já é mapeado pra 'paid'
               na canônica (ver REGRAS §2.2). Se o representante
               definir diferente, o filtro muda SÓ nas views — ETL
               e schema ficam iguais.
            5) META ADS: apenas level='campaign' no v_meta_campanha_daily.
               Adset e ad têm views de ranking próprias. Misturar níveis
               causaria double-counting (adset já soma em campaign).
            6) VISÃO GERAL NÃO INCLUI META.
               v_visao_geral_* só consulta `sales` (Loja Física +
               Nuvemshop). Meta Ads tem views próprias na aba dela.
               Reforça a decisão de 2026-04-10 sobre escopo da
               Visão Geral.
            7) SEGURANÇA POSTERGADA.
               Views são criadas sem policies de RLS. O service_role
               tem bypass e é o único que consulta por enquanto.
               Quando o frontend começar a consumir via anon/
               authenticated, avaliar: (a) expor views diretamente
               (dados agregados não são PII, pode ser aceitável) ou
               (b) criar policies explícitas. Decisão fica pra quando
               o frontend existir.
  Impacto:  14 views criadas em 20260411130000_dashboard_views.sql.
            Frontend fica simples: seleciona view, aplica WHERE de
            período, agrega com SUM/AVG conforme o card. Adicionar
            uma nova aba = nova view + novo WHERE clause, sem mexer
            em schema nem no ETL.

[2026-04-11] [ARQUITETURA] Stack do código das integrações (Caminho B)
  Contexto: a sessão da noite de 2026-04-11 começou o Caminho B
            (bases de code TS pré-ETL). Várias escolhas de stack
            precisavam ser travadas antes de criar o primeiro arquivo.
  Decisão:  Stack travada (testada com 109 unit tests passando):
            - Linguagem: TypeScript 5.7+ (strict + noUncheckedIndexedAccess
              + noUnusedLocals + noUnusedParameters)
            - Module system: ESM nativo ("type": "module" no package.json)
            - Runtime target: Node 24+ (usa fetch global, sem undici/axios)
            - Module resolution: Bundler (permite imports sem extensão .js)
            - Test runner: Vitest 2.x (globals: false — imports explícitos)
            - Mock HTTP: MSW 2.x (setupFiles com onUnhandledRequest: 'error')
            - Validação runtime: Zod 3.x com .passthrough() em tudo
            - Dependencias runtime: só zod (e playwright-core legado)
            - Dev deps: typescript, @types/node, vitest, msw
  Impacto:  tsconfig.json com noEmit: true (vitest handles TS diretamente).
            Qualquer integração futura (Meta Ads, outras fontes) segue
            esse mesmo padrão: src/integrations/{fonte}/{schemas,types,
            client,mapper,index}.ts + tests/integrations/{fonte}/*.test.ts.
            Migrar pra outra stack custa mais que manter.

[2026-04-11] [ARQUITETURA] Dependency Injection em todos os clients HTTP
  Contexto: os clients NuvemshopClient e ContaAzulClient poderiam ler
            credenciais direto de process.env, mas isso gera 3 problemas:
            (a) testes precisariam mockar process.env,
            (b) multi-store/multi-tenant fica impossível,
            (c) código de runtime fica acoplado ao ambiente.
  Decisão:  Zero leitura de process.env dentro dos clients. Constructor
            recebe um objeto de config com accessToken, userAgent, storeId
            (se aplicável). Quem instancia o client é responsável por ler
            env vars e passar.
  Impacto:  Tests ficam triviais (config fake inline), multi-store é
            1 linha (new Client({ ... })), e o código reaproveita em
            Edge Functions, Vercel serverless, scripts, tudo igual.
            O ContaAzulTokenManager segue o mesmo pattern — recebe
            clientId/clientSecret/refreshToken como config e uma callback
            onRefresh pra persistência injetada pelo caller.

[2026-04-11] [DADOS] PartialContaAzulSale com discriminator __partial
  Contexto: o endpoint /v1/notas-fiscais do Conta Azul (lista) só
            retorna metadata básico (data, numero, chave_acesso, nome,
            status). NÃO retorna valor total, items, forma de pagamento,
            vendedor_responsavel. Pra ter esses dados, precisa do
            endpoint de DETALHE por NF-e — que ainda não descobrimos
            durante live exploration (portal de docs bloqueado por WAF).
  Decisão:  Em vez de mapear prematuramente para CanonicalSale com 0
            de valor e items=[] (o que poluiria o banco com linhas
            incompletas), criamos um tipo separado PartialContaAzulSale
            com a flag `__partial: true` que o TypeScript impede de
            casar com CanonicalSale.
              interface PartialContaAzulSale {
                __partial: true;
                source: 'conta_azul';
                source_id: string;  // = chave_acesso
                sale_date: string | null;  // "0001-01-01" vira null
                status: SaleStatus;
                customer_name: string;
                numero_nota: number;
                chave_acesso: string;
              }
            O ETL futuro (T8) vai:
              1. Listar NF-e → partial sales
              2. Pra cada partial, chamar o endpoint de detalhe
              3. Merge partial + detail → CanonicalSale completo
              4. Inserir em `sales`
  Impacto:  Impossível inserir venda incompleta no banco por acidente.
            Custo: T7/T8 precisam primeiro DESCOBRIR o endpoint de
            detalhe (task T45 nova no backlog), depois escrever o merge.

[2026-04-11] [SEGURANCA] .env.local como fonte da verdade interina (até cofre existir)
  Contexto: a regra do CLAUDE.md §6 manda usar cofre 1P/BW como fonte
            da verdade de credenciais. Mas o cofre ainda não foi
            configurado, e durante a sessão de 2026-04-11 foram obtidas
            credenciais reais (Conta Azul PROD refresh_token, Nuvemshop
            access_token, store_id Miranda). Ficar esperando o cofre
            existir pra poder salvar esses valores era pior.
  Decisão:  Aceitar o .env.local local (gitignored) como cofre interino.
            O .gitignore foi blindado defensivamente (cofre.*, secrets.*,
            prints*/, /*.jpg, etc.) pra proteger contra vazamentos
            acidentais via git.
            ⚠ TRACK aberta: credenciais que passaram pelo chat durante
            a sessão (CONTA_AZUL_CLIENT_SECRET PROD, CONTA_AZUL_REFRESH_TOKEN,
            NUVEMSHOP_CLIENT_SECRET, NUVEMSHOP_ACCESS_TOKEN) devem ser
            rotacionadas ANTES do dashboard ir pra produção real com
            tráfego de usuário — task T47 nova no backlog.
  Impacto:  Próxima sessão PRECISA decidir: Bitwarden free (recomendo)
            ou 1Password family (pago). Sem isso, o sócio não consegue
            puxar credenciais na máquina dele sem passar tudo pelo chat
            de novo — multiplicando o risco de exposição. Task T46.

[2026-04-11] [ARQUITETURA] NF-e detail endpoint — discovery deferred para T8
  Contexto: durante live exploration do Conta Azul, várias variantes
            foram testadas pra listagem de vendas:
              /v1/vendas, /v1/sales, /v1/pedidos, /v1/pedido-venda  → 404
              /v1/venda  → 502 (405 internal, provavelmente POST-only)
              /v1/notas-fiscais  → 400 sem data, 200 com data_inicial+final
            O endpoint /v1/notas-fiscais funciona mas retorna metadata
            minimalista. O endpoint de DETALHE (que teria valor_total,
            items, vendedor_responsavel) não foi encontrado via brute-force.
            Tentamos docs oficiais via WebFetch e web search — portal
            developers.contaazul.com bloqueia WebFetch com 403 (WAF).
  Decisão:  Não gastar mais ciclos descobrindo na sessão de scaffolding.
            Quando começar T7/T8 (ETL real Conta Azul), o dev humano
            abre a doc no navegador (evita WAF) e descobre o endpoint
            correto. Possíveis suspeitos para testar primeiro:
              GET /v1/notas-fiscais/{chave_acesso}
              GET /v1/notas-fiscais/{numero_nota}
              GET /v1/notas-fiscais?chave_acesso={...}
              GET /v1/vendas/{id} (algum outro UUID?)
  Impacto:  Caminho B do Conta Azul ficou com um PartialContaAzulSale
            em vez de CanonicalSale completo. T7/T8 precisam resolver
            isso antes de popular o canonical `sales` com dados reais
            do Conta Azul.

[2026-04-12] [INTEGRACAO] Conta Azul NF-e retorna XML, nao JSON
  Contexto: ao testar GET /v1/notas-fiscais/{chave_acesso}, o endpoint
            retornou um documento XML (NF-e padrao SEFAZ) em vez de
            JSON como todos os outros endpoints da API v1. Isso era
            inesperado e nao esta documentado claramente no portal dev.
  Decisao:  Usar fast-xml-parser como dependencia para parsear o XML
            da NF-e. Parser dedicado criado em
            src/integrations/conta-azul/nfe-parser.ts. Extraimos:
            valor total, items (produto, quantidade, preco unitario,
            preco total), dados do destinatario (estado, cidade).
            REVERTIDA parcialmente a decisao de 2026-04-11 "NF-e detail
            endpoint — discovery deferred" — T45 agora esta RESOLVIDA.
  Impacto:  fast-xml-parser adicionada ao package.json como dep runtime.
            O PartialContaAzulSale agora pode ser completado com dados
            do XML e inserido como CanonicalSale no banco.

[2026-04-12] [INTEGRACAO] Conta Azul janela max de 15 dias no endpoint de lista
  Contexto: o endpoint GET /v1/notas-fiscais aceita data_inicial e
            data_final. Testes em producao mostraram que janelas maiores
            que 15 dias retornam erro ou resultados incompletos.
  Decisao:  ETL faz chunking automatico em janelas de 14 dias (margem
            de seguranca de 1 dia). Para full sync de 1 ano, sao ~26
            chunks sequenciais.
  Impacto:  Full sync demora ~8min (26 chunks x paginacao x XML parse).
            Incremental sync (ultimos 14 dias) demora ~10s. Cron de
            30 min usa modo incremental.

[2026-04-12] [INTEGRACAO] Refresh token persistence via etl_config table
  Contexto: o refresh_token do Conta Azul e single-use (cada uso gera
            um novo). Em GitHub Actions cron, nao ha disco persistente
            entre runs — salvar em .env.local nao funciona.
  Decisao:  Criar tabela etl_config no Supabase com colunas (key text PK,
            value text, updated_at timestamptz). O ETL le o refresh_token
            do etl_config antes de cada sync e escreve o novo token de
            volta apos cada refresh. Migration:
            supabase/migrations/20260412150000_etl_config_table.sql
  Impacto:  Resolve o problema de refresh_token em CI/cron. Qualquer
            config do ETL que precise persistir entre runs pode usar
            a mesma tabela. service_role tem acesso via RLS bypass.

[2026-04-12] [ARQUITETURA] Next.js 16 + Tailwind 4 + Recharts para frontend
  Contexto: precisavamos escolher a stack do frontend. A decisao de
            2026-04-10 mencionava "Next.js 15 + shadcn/ui + Recharts"
            como sugestao. Na pratica, Next.js 16 ja estava disponivel
            e Tailwind 4 e a versao corrente.
  Decisao:  Stack do frontend:
            - Next.js 16 com App Router
            - Tailwind CSS 4
            - Recharts para graficos (bar, pie, area)
            - @supabase/supabase-js para queries (service_role em
              Server Components — seguro porque e server-only code)
            - Sem shadcn/ui por enquanto — componentes proprios simples
              (KpiCard, RevenueChart, ChannelDonut, etc.)
  Impacto:  Frontend funciona em localhost:3000. Todas as paginas usam
            force-dynamic (export const dynamic = 'force-dynamic') para
            garantir dados frescos a cada request. Period filter via URL
            search params (7d/15d/30d/90d/1ano).

[2026-04-12] [DADOS] Views agrupam por product_name, nao product_id
  Contexto: ao rodar ETL real, 97% dos sale_items tinham product_id
            null. Isso acontece porque a Miranda nao preenche SKU no
            catalogo da Nuvemshop, e os items de NF-e do Conta Azul
            tambem nao tem referencia a produto cadastrado. As views
            originais agrupavam por products.product_id via JOIN,
            resultando em ranking vazio.
  Decisao:  DROP + CREATE das views v_*_produtos para agrupar
            diretamente por sale_items.product_name (sem JOIN com
            products). Migration:
            supabase/migrations/20260412120000_fix_product_views_group_by_name.sql
  Impacto:  Ranking de produtos funciona com dados reais. O campo
            products.sku continua existindo no schema para futuro uso,
            mas nao e a chave de agrupamento. Documentado em
            PENDENCIAS_MIRANDA.txt como item pendente da Miranda
            (preencher SKU no catalogo).

[2026-04-12] [ARQUITETURA] GitHub Actions cron para scheduler do ETL
  Contexto: precisavamos de um scheduler para rodar ETL incremental
            periodicamente. Opcoes avaliadas: Supabase Edge Functions
            + pg_cron, Vercel Cron, GitHub Actions. A decisao de
            2026-04-11 sugeria Supabase Edge Functions.
  Decisao:  Usar GitHub Actions scheduled workflows (cron) a cada 30
            minutos. Motivos:
            - Mais simples de configurar (YAML no repo)
            - Free para repos publicos
            - Nao depende de regiao (Supabase Edge Functions exigiria
              deploy separado, Vercel Cron exigiria regiao gru1)
            - Logs integrados ao GitHub
            Arquivo: .github/workflows/sync-nuvemshop.yml
            Roda ambos: sync:nuvemshop + sync:contaazul (incremental).
  Impacto:  Latencia de dados: ate 30 min entre a venda real e o dado
            aparecer no dashboard. Aceitavel para o caso de uso.
            GitHub Actions tem limite de 2000 min/mes no free tier
            (cron 30 min = ~1440 min/mes, dentro do limite).

[2026-04-12] [DADOS] normalizeState() para padronizar estados brasileiros
  Contexto: dados de NF-e vem com nome completo do estado ("Bahia",
            "Sao Paulo") enquanto dados da Nuvemshop vem com sigla
            ("BA", "SP"). Para consolidar geografia entre fontes,
            precisamos de um padrao.
  Decisao:  Funcao normalizeState() em src/lib/brazil.ts que mapeia
            nomes completos para siglas UF (27 estados). Cobertura
            de 93.5% dos registros reais testados. Casos nao mapeados
            ficam como o valor original.
  Impacto:  Todas as insercoes no campo customer_state passam por
            normalizeState(). Dashboard exibe UF padronizado.

[2026-04-13] [INTEGRACAO] App Marketing do Meta Ads criada sob conta FB alternativa
  Contexto: A conta dev@stepads.com.br nao foi aprovada pelo Facebook
            na tentativa de criar uma conta pessoal FB (Facebook
            rejeitou o cadastro). Sem conta FB, nao e possivel
            criar/administrar uma app no developers.facebook.com.
            Em paralelo, a Miranda (representante Lucca Cardoso)
            concedeu "Controle Total" no BM da Miranda (ID
            130431116357346) via convite por email para
            dev@stepads.com.br. O convite precisa ser aceito por uma
            conta FB logada — e o Facebook permite vincular o convite
            a QUALQUER conta FB que esteja logada no momento do clique,
            nao precisa corresponder ao email convidado.
  Decisao:  Usar uma conta FB alternativa (no momento: "Angelo
            Ribeiro") para aceitar o convite e operar o BM da Miranda.
            A app Marketing no developers.facebook.com sera criada sob
            essa mesma conta. O System User criado no BM da Miranda
            gera token permanente que continua funcionando
            independentemente da conta FB que criou a app — o token
            vive no BM, nao na conta individual.
  Impacto:  Ownership da app fica atrelada a conta FB que criou.
            Risco: se a conta FB for perdida (banimento do FB, pessoa
            sai da empresa, exclusao), perde-se o controle
            administrativo da app (nao da pra regerar app_secret,
            editar webhooks, submeter App Review). O token do System
            User continua funcionando ate rotacao manual.
            Mitigacao imediata (a fazer logo apos criar a app):
            adicionar um segundo admin FB em Settings > Roles >
            Administrators. Mitigacao de longo prazo: quando
            dev@stepads.com.br for aprovada pelo Facebook, transferir
            ownership da app pra ela. Documentar como divida em T47.

[2026-04-13] [OPERACIONAL] 2FA obrigatorio em qualquer conta FB com
             acesso ao BM da Miranda
  Contexto: Ao tentar aceitar o convite do BM da Miranda (permissao
            "Controle Total"), o Facebook bloqueia o fluxo na "Central
            de Contas" exigindo 2FA ativo na conta FB. Isso e
            politica do Meta para contas com permissoes elevadas em
            BMs — nao da pra contornar.
  Decisao:  Toda conta FB do revezamento Stepads que for operar o BM
            da Miranda precisa ter 2FA via app de autenticacao (Google
            Authenticator / Authy / Microsoft Authenticator). NAO usar
            2FA por SMS (quebra se trocar numero). Os codigos de
            recuperacao (10 codigos de uso unico) devem ser guardados
            no cofre Stepads (quando T46 sair) — por enquanto num txt
            local fora do repo.
  Impacto:  Onboarding de nova pessoa no revezamento requer setup de
            2FA antes de qualquer trabalho no Meta Ads. Adicionar isso
            ao setup inicial no CLAUDE.md secao 6 quando a app for
            criada e o fluxo estiver validado ponta-a-ponta.

[2026-04-13] [DADOS] Meta Ads unique constraint: drop expression
             index, trocar por constraint real em colunas simples
  Contexto: O schema original (migration 20260411120000) criou um
            unique index com expressao:
              create unique index uq_meta_insights on meta_ads_insights (
                date, level, campaign_id, coalesce(adset_id,''), coalesce(ad_id,'')
              );
            A intencao era tratar adset_id/ad_id NULL (em rows do
            nivel campaign, por exemplo) como tuplas unicas. Mas o
            Supabase JS client `.upsert({ onConflict: '...' })` so
            funciona com UNIQUE CONSTRAINT real em colunas SIMPLES —
            expression indexes nao sao alcancaveis via PostgREST
            `on_conflict`. Isso quebra o upsert do ETL.
  Decisao:  Migration 20260413120000_meta_ads_account_view.sql
            transforma adset_id/ad_id em NOT NULL DEFAULT '' e
            substitui o expression index por UNIQUE CONSTRAINT real:
              UNIQUE (date, level, campaign_id, adset_id, ad_id)
            Semantica preservada: '' = "nao aplicavel pro nivel"
            (campaign rows tem adset_id='' e ad_id=''). Views
            existentes ja filtram por level, entao '' nao aparece
            nos rankings.
  Impacto:  Upsert do ETL Meta Ads funciona via Supabase JS. O
            mapper continua produzindo null no canonical type (boa
            semantica), mas o sync converte pra '' no boundary do
            upsert. Futuro: se alguma feature precisar de "rows
            tipicamente sem adset_id", verificar se esse default
            atrapalha.

[2026-04-13] [FRONTEND] Meta Ads fora do faturamento da Visao Geral
             (reforco)
  Contexto: A regra ja existia (CLAUDE.md 3.1 + DECISOES 2026-04-10
            "Escopo da Visao Geral"). Ao implementar a aba Meta Ads
            em 2026-04-13, foi facil esquecer e somar spend/
            purchase_value no KPI total do dashboard.
  Decisao:  A aba Meta Ads tem um disclaimer de topo (faixa amber)
            reforcando que Meta Ads e ATRIBUICAO de midia, nao
            venda. O valor de compras do Meta NAO eh somado no
            faturamento da Visao Geral. Venda real vem apenas de
            Loja Fisica (Conta Azul) + Nuvemshop.
  Impacto:  Nenhum no codigo (regra ja respeitada nas queries e
            views de Visao Geral). O disclaimer visual evita que
            o usuario final interprete errado os numeros da aba.

[2026-04-13] [DADOS] Meta Ads: purchase action_types sao aliases, nao
             somar — usar pickFirstActionByPriority
  Contexto: O mapper original (T25) somava 5 action_types de purchase
            (purchase, offsite_conversion.fb_pixel_purchase,
            omni_purchase, onsite_web_purchase, web_in_store_purchase)
            assumindo que seriam tipos distintos de compra. Na pratica,
            a API Meta retorna o MESMO evento sob todos os 5 aliases
            simultaneamente, com valores identicos. Resultado: compras
            e purchase_value estavam 5x inflados no banco, e ROAS
            consequentemente 5x maior que o real.
  Decisao:  Substituido sumActionsByType() por pickFirstActionByPriority()
            em src/integrations/meta-ads/mapper.ts. O mapper agora
            percorre os tipos em ordem de prioridade (canonico =
            "purchase" primeiro) e retorna o valor do PRIMEIRO encontrado.
            Se a Miranda usar um Pixel que so emite um tipo nao-canonico,
            o fallback pega automaticamente.
  Impacto:  Todos os dados de Meta Ads no Supabase foram re-sincronizados
            apos o fix (npm run sync:meta-ads:full). Validacao cruzada
            com API confirmou MATCH EXATO (Apr 6-12: 17 purchases,
            R$5932.97). ROAS agora reflete o valor real.

[2026-04-13] [DADOS] Seed data de meta_ads_insights removida do Supabase
  Contexto: O seed.sql continha uma campanha ficticia "Coleção Outono
            2026" (ID 23847562341) com datas relativas (now() - interval
            '7 days'). Essa seed se movia com o tempo e poluia a janela
            recente do dashboard, somando spend e compras inexistentes
            aos dados reais. A campanha nao existe na API Meta.
  Decisao:  Deletados 35 registros com campaign_id='23847562341' do
            Supabase. O seed.sql nao foi alterado (ele so roda em
            setup inicial e seria sobrescrito pelo ETL real de qualquer
            forma), mas se alguem rodar seed novamente, devera limpar
            meta_ads_insights depois.
  Impacto:  Spend e impressions nos dias Apr 4-10 estavam inflados
            (ex: Apr 6 mostrava R$218,52 em vez de R$73,52 real).
            Apos limpeza + re-sync, todos os valores batem com a API.

[2026-04-13] [ARQUITETURA] PostgREST limit explicito nas queries Meta Ads
  Contexto: PostgREST (camada REST do Supabase) impoe limit default de
            1000 rows em TODOS os planos (free, pro, enterprise). Queries
            sem .limit() explicito sao truncadas silenciosamente. As
            queries de ranking Meta Ads consultam tabelas base agregando
            em JS. Com 9+ campanhas e filtro de 1 ano (365 dias),
            fetchMetaCampaignRanking teria 3285 rows truncadas em 1000,
            produzindo rankings incorretos.
  Decisao:  Adicionado .limit(10000) em fetchMetaDaily,
            fetchMetaCampaignRanking e fetchMetaAdRanking.
  Impacto:  Rankings agora corretos para qualquer periodo de filtro.

[2026-04-13] [ARQUITETURA] Retention policy de 90 dias em raw_meta_insights_*
  Contexto: O sync.ts insere payloads brutos em raw_meta_insights_campaign,
            raw_meta_insights_adset e raw_meta_insights_ad a cada run do
            ETL (append-only, sem dedup). Com cron de 30 min (48 runs/dia)
            e ~238 rows/run, as tabelas cresceriam ~11k rows/dia ou ~4.2M
            rows/ano. No plano free do Supabase (500 MB), isso atingiria
            o limite em meses.
  Decisao:  Ao final de cada syncMetaAds(), rodar cleanupRawTables() que
            deleta registros com date_start < (hoje - 90 dias). 90 dias
            eh suficiente para debugging e auditoria.
  Impacto:  Raw tables estabilizam em ~(34 campanhas * 90 dias * 48 daily
            writes)/dedup ≈ tamanho gerenciavel. Se precisar de historico
            bruto maior, ajustar RAW_RETENTION_DAYS em sync.ts.

[2026-04-13] [DADOS] PostgREST limit explicito em TODAS as queries diarias
  Contexto: Alem das queries de Meta Ads (corrigidas acima), as queries
            de Visao Geral e Nuvemshop TAMBEM estavam sendo truncadas.
            v_visao_geral_daily tinha 2000+ rows (885 dias NS + 115 CA
            x 2 fontes) e v_nuvemshop_daily tinha 1820 rows. Ambas
            truncadas silenciosamente em 1000 pelo PostgREST default.
            fetchDailyRevenue(period.days * 2) com filtro de 1 ano
            retornava 1000 rows em vez de 1460 → KPIs de comparacao
            incorretos na Visao Geral.
  Decisao:  Adicionado .limit(10000) em fetchDailyRevenue e
            fetchNuvemshopDaily. fetchMonthlyComparison ja tinha
            paginacao propria (nao afetado).
  Impacto:  Corrige KPIs de comparacao e graficos na Visao Geral,
            Nuvemshop e Loja Fisica para periodos longos (1 ano).

[2026-04-13] [ARQUITETURA] Timezone: parsePeriod() agora usa
             America/Sao_Paulo explicitamente
  Contexto: CLAUDE.md regra 3.2 diz "Tudo em America/Sao_Paulo".
            Porem, parsePeriod() usava new Date() que no Vercel
            (server em UTC) calculava "hoje" como UTC. Apos 21h SP
            (00h UTC), o dia corrente no dashboard avancava 1 dia
            em relacao ao banco (que armazena datas em SP). Isso
            causava off-by-one nos filtros de periodo e comparacao.
            Mesmo problema nos filtros de periodo anterior das 4
            abas: usavam new Date(r.day) que parseia como UTC.
  Decisao:  parsePeriod() agora usa Intl.DateTimeFormat com
            timeZone='America/Sao_Paulo' (sem dependencia externa).
            Filtros de periodo anterior nas 4 abas agora comparam
            strings YYYY-MM-DD diretamente (r.day < period.since)
            em vez de construir Date objects.
  Impacto:  Dashboard inteiro agora consistente com timezone SP.
            Sem off-by-one em nenhum horario do dia.

[2026-04-13] [INTEGRACAO] Conta Azul: saveRefreshToken deve LANCAR erro
  Contexto: O refresh_token do Conta Azul e single-use. Se o ETL consome
            o token antigo, recebe um novo, mas falha ao salvar no
            Supabase, o token novo se perde. Proximo run falha
            permanentemente (token antigo ja foi invalidado).
  Decisao:  saveRefreshToken() agora faz throw em vez de console.error
            quando o upsert falha. O erro propaga e o cron reporta
            falha, permitindo intervencao antes do token expirar.
  Impacto:  Cron pode falhar se Supabase estiver fora, mas isso e
            preferivel a perder o token silenciosamente.

[2026-04-13] [DADOS] Conta Azul: fix off-by-one no chunking de NF-e
  Contexto: O ETL quebra o periodo em janelas de 14 dias. A logica
            original calculava windowEnd = start + 14 dias, depois
            avancava start = windowEnd + 1 dia. NF-e emitidas
            exatamente no windowEnd caiam em gap de 24h entre janelas.
  Decisao:  Alterado para (MAX_WINDOW_DAYS - 1) no calculo de
            windowEnd, garantindo janelas de 13 dias inclusivos
            (ex: Apr 1-13, Apr 14-27). Sem gap.
  Impacto:  Recupera vendas que estavam sendo puladas em boundaries.

[2026-04-13] [DADOS] Conta Azul: quantidade fracionaria preservada
  Contexto: NF-e permite quantidades fracionarias (1.5L, 0.25kg).
            O ETL usava Math.round() na quantidade, arredondando
            1.5 para 2 e corrompendo o calculo de receita por item.
  Decisao:  Removido Math.round(). Coluna quantity no banco aceita
            numeric, entao valores fracionarios sao armazenados
            corretamente.
  Impacto:  Ranking de produtos e receita por item ficam precisos.

[2026-04-13] [ARQUITETURA] GitHub Actions: fail-fast entre ETL steps
  Contexto: Se o step de Nuvemshop falhasse, Conta Azul e Meta Ads
            rodavam mesmo assim, desperdicando minutos do Actions
            e mascarando o erro original.
  Decisao:  Adicionado if: success() nos steps de Conta Azul e
            Meta Ads. Se qualquer step anterior falhar, os
            subsequentes sao pulados.
  Impacto:  Cron reporta falha mais rapidamente e nao executa
            trabalho desnecessario.

[2026-04-14b] [PRODUTO] Ranking de criativos com thumbnail + leads/CPL
  Contexto: Miranda pediu (com imagem de referencia do Ads Manager)
            que o ranking de criativos mostrasse thumbnail da imagem
            do anuncio + colunas Leads e CPL alem das existentes de
            Compras/Receita/ROAS.
  Decisao:  Implementado em 3 camadas:
            - DB: migration 20260414160000 cria tabela
              meta_ads_creatives (ad_id PK, thumbnail_url, image_url)
              + colunas leads/leads_value em meta_ads_insights.
            - ETL: novo metodo listAdCreatives no client (hit em
              /act_<id>/ads?fields=id,name,creative{thumbnail_url,
              image_url}) + syncCreatives rodando no fim de toda
              syncMetaAds. Mapper extrai leads via
              LEAD_ACTION_PRIORITY (mesma logica de
              pickFirstActionByPriority pra evitar overcount).
            - Frontend: componente novo CreativeRankingTable usa
              <img> puro (nao next/image) porque os dominios CDN
              do Meta sao variaveis (scontent-*.xx.fbcdn.net).
              Thumb 40x40 arredondada.
            - Workflow: input full_meta_sync no workflow_dispatch
              pra backfill manual de 90 dias.
  Impacto:  Aba Meta Ads com referencia visual pro usuario final.
            Pipeline de thumbnail + leads pronto caso Pixel da
            Miranda comece a emitir eventos de Lead.

[2026-04-14b] [PRODUTO] Remover colunas Leads/CPL do frontend
  Contexto: Apos rodar full sync de 90 dias (via workflow_dispatch
            com full_meta_sync=true), auditoria no banco mostrou
            leads=0 em 100% das linhas de meta_ads_insights. A query
            "leads > 0" retornou vazio em toda a janela de 90 dias
            em todos os 3 niveis (campaign/adset/ad). O Pixel da
            Miranda nao configura eventos de Lead — so rastreia
            Purchase. A imagem de referencia que a Miranda enviou
            era de uma conta diferente (provavelmente um cliente
            de captacao de leads).
  Decisao:  Remover colunas Leads e CPL do componente
            CreativeRankingTable (inclusive a interface
            CreativeRankingRow). MANTER mapper, migration e sync
            pipeline intactos — se a Miranda um dia configurar
            evento Lead no Events Manager (ex: formulario de
            contato), basta re-adicionar os <th>/<td> no componente.
  Impacto:  Aba Meta Ads fica visualmente limpa (7 colunas em vez
            de 9). Zero trabalho perdido — o backend continua
            preparado.

[2026-04-14c] [DADOS] 🚨 CRITICO: ETL Conta Azul puxa so NF-e, perde PDV
  Contexto: Miranda apresentou o fechamento oficial de marco 2026
            (reuniao de apresentacao do dashboard):
              - Faturamento Total:  R$ 116.544,00
              - Loja Fisica (CA):   R$ 82.944,79
              - Site (NS):          R$ 33.599,21
            Auditoria no Supabase via REST API mostrou:
              - Nuvemshop marco:    R$ 33.599,21 (29 dias, 92 pedidos)
                → MATCH EXATO NOS CENTAVOS
              - Conta Azul marco:   R$ 20.187,58 (4 dias, 38 NF-e)
                → FALTANDO R$ 62.757 (75% dos dados)
            Cobertura CA esparsa em TODOS os meses:
              nov/25: 9 dias, dez/25: 14, jan/26: 14, fev/26: 7,
              mar/26: 4, abr/26: 5 — vs NS: 26-30 dias/mes.
            Perfil das 38 NF-e de marco: 100% pago por transferencia
            bancaria, clientes identificados com nome completo, de
            varios estados (SP/RJ/MG/SC/CE/PR). Isso eh perfil de
            venda B2B, envio remoto ou pedidos NS duplicados como
            NF-e — nao eh PDV de balcao.
            Causa raiz: src/etl/conta-azul/sync.ts:130 chama apenas
            /v1/notas-fiscais com filtro status=EMITIDA. Vendas PDV
            saem em NFC-e (cupom fiscal) ou como "venda" interna no
            Conta Azul sem documento fiscal — 100% invisiveis pro
            ETL. MAPEAMENTO_CONTA_AZUL.txt:436 ja alertava em
            2026-04-10 que "grande parte das vendas da loja fisica
            nao tem cliente vinculado", mas o ETL foi implementado
            em cima de /v1/notas-fiscais porque era o unico endpoint
            que respondia 200 na epoca (api-v1 com /v1/vendas,
            /v1/sales, /v1/pedidos deram 404).
  Decisao:  Abrir T49 no backlog com criticidade MAXIMA. Proximo
            passo: identificar o endpoint correto na api-v2 via
            (a) testar com credenciais reais, (b) humano abrir
            developers.contaazul.com (WAF bloqueia Claude), ou
            (c) perguntar a Miranda qual relatorio ela usa. Uma
            vez identificado, estender sync.ts pra chamar o novo
            endpoint em PARALELO a /v1/notas-fiscais (com dedup
            por source_sale_id ou tupla data+valor+cliente pra
            evitar double-counting quando a venda tambem virou
            NF-e). Rodar sync full historico e validar com Miranda.
            Comunicar aos stakeholders (feito nesta sessao) que os
            numeros historicos da Loja Fisica e Visao Geral estavam
            subestimados.
  Impacto:  Aba Loja Fisica e Visao Geral estavam mostrando ~25% do
            faturamento real em periodos historicos. Nuvemshop e
            Meta Ads nao sao afetadas. Decisao CLAUDE.md regra 3.1
            (Visao Geral = CA + NS) continua valida — so a fonte CA
            estava incompleta.

[2026-04-14d] [INTEGRACAO] ETL Conta Azul reescrito: NF-e → API de Vendas
  Contexto: T49 identificou que /v1/notas-fiscais capturava apenas ~25%
            das vendas da loja fisica. Investigacao da API v2 descobriu
            /v1/venda/busca que retorna TODAS as vendas (PDV, balcao,
            link de pagamento), com detalhe via /v1/venda/{id} e itens
            via /v1/venda/{id}/itens. Probes confirmaram: 100% das
            vendas sem origem tem vendedor="LOJA FISICA", com itens e
            dados de pagamento acessiveis.
  Decisao:  Substituir integralmente o ETL de NF-e pelo ETL de Vendas.
            - sync.ts reescrito para /v1/venda/busca
            - Filtro: exclui vendas com origem contendo "nuvem" (NS)
            - 110 registros NF-e antigos deletados (flag --migrate)
            - Full sync 180d: 496 vendas, 900 itens, 0 errors
            - Mapa Brasil removido da aba Loja Fisica (API de vendas
              e /v1/pessoas nao retornam endereco do cliente)
            PENDENTE: Miranda precisa confirmar se fecha o mes por
            data da venda ou data do recebivel. Mensagem enviada.
  Impacto:  Cobertura subiu de 4 para 20 dias/mes. Dados de produtos,
            clientes e pagamentos agora disponiveis para loja fisica.
            Geografia perdida (NF-e trazia UF do destinatario, API de
            vendas nao). NF-e parser (nfe-parser.ts) fica como dead
            code mas preservado para historico.

[2026-04-15] [BUG] Fix TZ no sale_date + cleanup de sales fantasma
  Contexto: Apos a reescrita de 2026-04-14d, dashboard passou a
            mostrar R$ 84.423,90 em marco 2026, enquanto Miranda
            reportava R$ 82.944,79. Diff de +R$ 1.479,11 — aparente
            "sobra". Investigacao via scripts diagnosticos
            (src/scripts/debug-marco-*.ts) isolou 2 bugs:
            1. sync.ts gravava sale_date = "YYYY-MM-DD" (string pura
               do data_vencimento). Supabase interpreta strings de
               date como UTC midnight. A view v_visao_geral_daily
               aplica "at time zone 'America/Sao_Paulo'" antes de
               truncar ::date, shiftando 3h pra tras. Resultado:
               recebivel com venc 01/04 virava dia 31/03 SP e
               entrava em marco. 15 sales impactados, R$ 4.489,50.
            2. createSaleFromRecebivel criava sale com status='paid'
               mesmo quando totalPago=0. 194 sales fantasma com
               gross=0 (194 em todo banco, 59 especificamente em
               marco 2026). Nao afetam soma de receita mas poluem
               contagem de pedidos.
  Decisao:  (a) sync.ts: gravar sale_date como "${dt}T03:00:00Z"
                (SP midnight) + skip if grossRevenue <= 0 em ambas
                funcoes (processVenda e createSaleFromRecebivel).
            (b) Update massa retroativo no Supabase: +3h em todos
                2548 sales CA + delete dos 194 fantasmas. Marcado
                etl_config "ca_tz_fix_applied" pra idempotencia.
            (c) Gap residual de R$ 3.010,39 (pos-fix marco = R$
                79.934,40 vs target R$ 82.944,79) deixado pendente
                ate ter acesso a API CA pra diagnosticar. Hipoteses
                documentadas em HANDOFF secao 0.
  Impacto:  Marco 2026 agora mostra numero "honesto" em vez do
            inflado coincidentemente mais proximo do target. Outros
            meses historicos tambem sao afetados pelo mesmo shift,
            entao todos os numeros de Loja Fisica e Visao Geral
            mudaram ligeiramente. NF so usa timestamp real dos
            pedidos (nao date string), nao foi afetada. Regra nova:
            qualquer fonte que grave sale_date a partir de date
            string pura deve offsetar para SP midnight ou arriscar
            o mesmo shift.

[2026-04-15] [UX] Calculo do periodo anterior nos KPI cards (% change)
  Contexto: Miranda reportou que as porcentagens no canto superior
            direito dos KPI cards estavam muito mais elevadas do que
            o esperado. A investigacao mostrou que o codigo
            calculava o "periodo anterior" via
              fetchDailyRevenue(days * 2) + filter(r.day < since)
            Esse padrao funciona pro caso default de "Ultimos N dias"
            porque busca 2N dias a partir de HOJE e depois separa em
            dois blocos de N dias. Mas quebrava em 2 cenarios:
              (a) date picker custom (ex: "Mar 2026"). days*2 = 60
                  buscava Feb 14 → Apr 15. Filter day<Mar 1 deixava
                  so 12 dias de Fev como "periodo anterior" vs os 31
                  dias do atual → comparacao injusta inflacionando %.
              (b) filtro "1 ano" (365d). O periodo anterior caia antes
                  de Nov 2025 (data floor do nosso sync) → prev = 0
                  → percentChange(current, 0) retornava 100 → todo
                  card mostrava "+100%" falso.
            Validacao paralela: cruzamos dados do banco com a API CA
            via /v1/venda/busca.totais.aprovado para Nov 2025 - Abr
            2026. Diff foi R$ 0,00 em todos os meses. Ou seja, nao ha
            inflacao nos dados historicos — sao reais da API. A
            expectativa de +38% da Miranda deve se referir a um
            comparativo que nao temos (provavel: Mar 2025 vs Mar 2026
            ano-a-ano, e nossos dados comecam em Nov 2025).
  Decisao:  (a) novo helper getPreviousPeriod(since, until) em
                queries.ts computa a janela anterior de MESMO tamanho
                imediatamente antes de [since, until]. Ex:
                [Mar 1, Mar 31] → [Jan 30, Feb 28].
            (b) 3 pages (Visao Geral, Loja Fisica, Nuvemshop) passaram
                a fazer o fetch do periodo anterior com prevSince e
                prevUntil explicitos em vez de days*2 + filter.
            (c) percentChange() retorna null quando previous === 0
                em vez de 100. KpiCard ja nao renderiza badge pra
                null, entao cards ficam sem % em vez de mostrar
                "+100%" enganoso quando nao ha dado pra comparar.
  Impacto:  KPI cards mostram % corretas pra qualquer escolha de
            periodo. Date picker custom ("Marco 2026") compara com
            janela de mesmo tamanho imediatamente antes ("Jan 30 -
            Feb 28"). "1 ano" nao mostra mais +100% falso — mostra
            simplesmente sem badge. Loja Fisica aba continua usando
            a formula CA - NS pra revenue e orders no prev periodo
            (subtracao espelhada). Esse fix nao resolve a discrepancia
            das % que Miranda mencionou (+42%/+13%/+38%) — isso e um
            comparativo diferente. O que foi corrigido eh que as %
            exibidas sao agora matematicamente corretas e comparam
            janelas equivalentes.

[2026-04-16] [UX] Comparativo Mensal — mes parcial compara mesmo periodo
  Contexto: O componente MonthlyComparison mostrava o mes atual (Abril
            2026, com 15 dias de dados) comparado com o mes anterior
            inteiro (Marco, 31 dias). Resultado: -52% artificial em
            Abril. Nao refletia "esta caindo" — refletia "ainda faltam
            dias pra terminar o mes".
  Decisao:  fetchMonthlyComparison() passa a detectar o mes cronologi-
            camente mais recente no banco. Se o `lastDay` dele for antes
            do ultimo dia do calendario, eh marcado como `partial: true`.
            Pro mes parcial, o % anterior compara APENAS os primeiros N
            dias do mes anterior (onde N = vals.lastDay do mes parcial).
            Ex: Abr 1-15 (R$ 55.5K) vs Mar 1-15 (R$ 54.2K) = +2.4%.
            Componente ganhou badge amarelo "parcial" ao lado do nome
            do mes + legenda embaixo explicando a convencao.
            MonthlyData.partial: boolean foi adicionado ao interface.
  Impacto:  Comparativo Mensal nao mostra mais queda enganosa no mes
            atual. Meses passados continuam comparados mes-cheio vs
            mes-cheio. O total do ano no rodape ainda inclui o mes
            parcial, mas isso eh esperado (total YTD).

[2026-04-16] [DADOS] Expansao do historico CA de 6 meses pra 3 anos
  Contexto: Sync original da Conta Azul foi 180 dias (Nov 2025-Abr 2026),
            entao o Comparativo Mensal so conseguia mostrar anos 2025 e
            2026. Probe da API /v1/venda/busca mostrou que a CA tem
            dados disponiveis desde Jan 2023 (~9600 vendas, R$ 3.68M).
  Decisao:  Rodar sync historico adicional pros meses Jan 2023 a Out 2025
            (Nov 2025+ ja tem). Vai dar ao Comparativo Mensal 41 meses
            de historico (3+ anos). Estrategia: rodar em 6 chunks de 6
            meses em sequencia (nao paralelo — refresh_token CA eh
            single-use). Cada chunk ~15-20 min. Total ~2h.
            Comandos:
              npm run sync:contaazul -- --from 2023-01-01 --to 2023-06-30
              npm run sync:contaazul -- --from 2023-07-01 --to 2023-12-31
              ...ate 2025-10-31
            Sessao 2026-04-16 iniciou o sync mas precisou interromper em
            225/9379 vendas pra handoff de maquina. Upsert idempotente
            permite continuar sem duplicar.
  Impacto:  Dashboard vai ter comparativo de 3+ anos. Miranda podera ver
            tendencias ano-a-ano. Tempo de sync eh pontual — depois, o
            sync incremental (cron 30min) so puxa dados novos.


[2026-04-16b] [DADOS] Sync historico CA concluido (3 anos completos)
  Contexto: A entrada 2026-04-16 deixou o sync historico iniciado mas
            parado em 550/9379 vendas. Esta sessao (outra maquina)
            retomou e completou os 6 chunks em sequencia.
  Decisao:  Executado conforme plano: set -e + && chained, logando em
            /tmp/miranda-sync/log.txt, com Monitor capturando SYNC
            COMPLETE e errors via stream. Cada chunk validado imediato
            (API totais.aprovado bate com Sales upserted no chunk).
            Validacao final via src/scripts/validate-historic-ca.ts
            (novo, reusavel): diff R$ 0,00 por chunk, total R$
            3.678.248,49 batendo exato com o reportado pela API.
  Impacto:  Dashboard > Comparativo Mensal agora cobre 2023-2026 (41
            meses). Total CA no banco: 2023 R$ 1.057.032,74, 2024 R$
            1.324.087,85, 2025 R$ 1.664.426,18 (inclui Nov+Dez
            pre-existentes), 2026 R$ 716.902,30 (parcial ate Abr).
            Tempo real foi ~3h 20min (60% acima da estimativa de 2h
            do handoff) — nao travou em nenhum chunk, so eh API mais
            lenta do que esperado. Sequential execution funcionou
            perfeitamente — zero conflito de refresh_token.

[2026-04-16b] [DADOS] Bug coluna sale_items.quantity INT (T52 aberto)
  Contexto: Durante o sync historico, 4 items (de ~18k) falharam com
            "invalid input syntax for type integer" — a API CA retorna
            quantidades fracionarias como "2.04", "2.7", "3.2", "7.02"
            (provavelmente kg/L de materiais de papelaria/artesanato).
            A coluna sale_items.quantity esta declarada INT desde a
            migration inicial 20260411120000.
            O audit 2026-04-13b (item C3) ja havia removido o
            Math.round(quantidade) no mapper pra preservar precisao,
            mas o schema do DB nao foi ajustado. Discrepancia ficou
            latente ate o sync historico expor em escala.
  Decisao:  Registrar como T52 no backlog. Nao fixar agora porque:
            (a) Sales sao gravadas normalmente — faturamento nao
                afetado.
            (b) Items perdidos = 0.02% (4 em 18k) — ranking de
                produtos continua valido.
            (c) Fix requer migration + resync dos meses 2023-H2 e
                2025-H1 (~70 min de sync adicional).
            Proposta quando for feito:
              ALTER TABLE sale_items ALTER COLUMN quantity TYPE
                numeric(12,3);
            Mais resync dos 2 chunks afetados. Views que fazem
            sum(si.quantity)::int continuam valendo (o cast final
            arredonda na agregacao).
  Impacto:  Divida tecnica documentada. Prioridade baixa.
